1. 리눅스에서 현재 구동중인 프로세스 리스트를 보고자 한다.
   입력해야할 명령어는 무엇인가 ?

ps -ef

2. 특정 프로세스(a.out)를 찾아내고자 할 때 사용하는 명령어를 기술하시오.
   이 때 찾는 프로세스 외의 다른 프로세스는 나오면 안된다.

ps -ef | grep a.out | grep -v grep

3. top 명령어의 역할은 무엇인가 ?

실시간으로 CPU 사용률을 체크해주는 도구로 어떤 프로세스가 CPU 를 과다하게 사용하는지 분석이 가능함

4. 아래 명령어에 대해 설명하시오.
   ps -ef | grep a.out | grep -v grep | awk '{print $2}'

a.out 이라는 프로세스의 PID 번호를 찾는 명령어

5. ls -alR 명령어에 대해 설명하시오.

현재 위치에 있는 모든 파일과 디렉토리, 디렉토리 밑에 있는 파일과 디렉토리를 모두 출력하고
각 파일 및 디렉토리의 권한과 파일의 종류에 대해 출력해준다.

6. 최신 버전의 커널에서는 무엇을 하면 데몬이 되는지 기술하시오.
   아주 단순한 커맨드 하나로 이를 수행 할 수 있다.

프로그램을 실행할 때 & 를 붙여 background 로 구동시키면 된다.

7. 리눅스에서 사용하는 장치 파일은 부팅 순간에 올라간다.
   이 관점에서 insmod 가 하는 역할이 무엇인지 기술하시오.

insmod 는 장치를 구동시키기 위한 SW 를 커널에 등록해주는 역할을 수행한다.

8. rmmod 의 역할에 대해 기술하시오.

rmmod 는 현재 올라가있는 장치 SW 를 커널에서 빼내는 작업을 수행한다.

9. lsmod 의 역할에 대해 기술하시오.

현재 어떠한 장치 SW 가 커널에 등록되어 있는지 살펴보는 역할을 수행한다.

10. strace 명령어가 하는 일을 기술하시오.

특정 프로그램의 구동에서 어떠한 시스템 콜들이 호출되는지를 분석해줌