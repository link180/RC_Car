1. 프로그램으로 a.txt 파일을 만들어보자!
   전체 구동 코드일 필요 없고 핵심적인 부분만 몇 개 들어가면 된다.
   전체 구동이어도 무방하니 편한대로 구현하라.

int fd;
fd = open("a.txt", O_RDWR, 0644);

2. git 에 있는 저장소를 다운로드하는 방법에 대해 기술하시오.
   명령어를 작성하면 됨

git clone 저장소URL

3. 협업을 하는데 있어 git 이 좋은 이유를 설명하시오.

갑자기 컴퓨터가 날아가는 일이라든지
쓸대없이 동일한 코드를 똑같이 수정하고 있는다던지 하는 불상사를 막아주고 거기서 발생하는 문제를 최소화시켜줌

4. a.txt 파일을 읽는 프로그램을 작성하시오.
   코드 전체일 필요 없이 핵심적인 부분만 표현되면 됨

int fd, nr;
char buf[256] = {0};
fd = open("a.txt", O_RDONLY);
nr = read(fd, buf, sizeof(buf));

5. a.txt 파일에 내용을 기록하는 프로그램을 작성하시오.
   코드 전체일 필요 없이 핵심 로직만 표현되면 됨

int fd;
char buf[256] = "Write Test";
fd = open("a.txt", O_WRONLY);
write(fd, buf, sizeof(buf));

6. 백그라운드 실행 없이 데몬 만드는 방법을 기술하시오.

우선 부모 프로세스를 죽이고 파일을 모두 닫은 다음 자기 자신을 세션 id 로 만들면 된다.

7. 리눅스에서 signal() 사용법을 기술하시오.

signal(시그널 번호, 시그널 핸들러) 형태로 사용한다.

8. 다음 코드를 해석하시오.
   signal(SIGINT, int_handler);

SIGINT 신호가 발생하면 즉 Ctrl + C 를 누르면
int_handler 함수가 동작한다.

9. wait() 를 통해 종료 코드를 받아왔다.
   이를 분석하는 방법에 대해 기술하시오.

상위 8 비트와 하위 8 비트에 따라 정상 종료 혹은 signal 기반 종료가 분류된다.

10. waitpid() 의 유용성을 기술하시오.

논 블로킹 동작이므로 블록되지 않고 다른 작업을 수행할 수 있음